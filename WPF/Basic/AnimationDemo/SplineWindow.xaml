<Window x:Class="AnimationDemo.SplineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AnimationDemo"
        mc:Ignorable="d"
        Title="SplineWindow" Height="450" Width="800">

    <Window.Resources>
        <Storyboard
            x:Key="sb">
            <DoubleAnimationUsingKeyFrames
                Storyboard.TargetName="tt"
                Storyboard.TargetProperty="X"
                AutoReverse="True">
                <SplineDoubleKeyFrame
                    KeyTime="0:0:3"
                    Value="375"
                    KeySpline="0.2,0.3 0.3,0.8" />
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </Window.Resources>
    
    <Window.Triggers>
        <EventTrigger
            RoutedEvent="Button.Click">
            <BeginStoryboard
                Storyboard="{StaticResource sb}" />
        </EventTrigger>
    </Window.Triggers>
    
    <Grid>
        <StackPanel>
            <Border
                Background="Yellow"
                Width="400"
                Height="30"
                Name="border">
                <Ellipse
                    Width="25"
                    Height="25"
                    Stroke="White"
                    Fill="White"
                    HorizontalAlignment="Left">
                    <Ellipse.RenderTransform>
                        <TranslateTransform
                            X="0"
                            x:Name="tt" />
                    </Ellipse.RenderTransform>
                </Ellipse>
            </Border>
            <Button
                Content="开始动画"
                Name="btn_Start" />
        </StackPanel>
    </Grid>

</Window>
