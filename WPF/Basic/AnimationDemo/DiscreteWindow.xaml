<Window x:Class="AnimationDemo.DiscreteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AnimationDemo"
        mc:Ignorable="d"
        Title="DiscreteWindow" Height="450" Width="800">

    <Window.Resources>
        <Storyboard
            x:Key="sb">
            <StringAnimationUsingKeyFrames
                Storyboard.TargetName="txtb_Info"
                Storyboard.TargetProperty="Text">
                <DiscreteStringKeyFrame
                    KeyTime="0:0:0"
                    Value="H" />
                <DiscreteStringKeyFrame
                    KeyTime="0:0:0.5"
                    Value="He" />
                <DiscreteStringKeyFrame
                    KeyTime="0:0:1"
                    Value="Hel" />
                <DiscreteStringKeyFrame
                    KeyTime="0:0:1.5"
                    Value="Hell" />
                <DiscreteStringKeyFrame
                    KeyTime="0:0:2"
                    Value="Hello" />
            </StringAnimationUsingKeyFrames>
        </Storyboard>
    </Window.Resources>
    
    <Window.Triggers>
        <EventTrigger
            RoutedEvent="Button.Click">
            <BeginStoryboard
                Storyboard="{StaticResource sb}" />
        </EventTrigger>
    </Window.Triggers>
    
    <Grid>
        <StackPanel>
            <TextBlock
                Name="txtb_Info" />
            <Button
                Content="开始动画"
                Name="btn_Start" />
        </StackPanel>
    </Grid>

</Window>
