<Window
    x:Class="Navigate.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Navigate.Views"
    mc:Ignorable="d"
    Title="MainView"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:Navigate.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
    xmlns:common="clr-namespace:Navigate.Common"
    Height="450"
    Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel
            Orientation="Vertical">
            <Button
                Content="Navigate To AAAView：基础的区域管理和导航"
                Margin="10"
                FontSize="18"
                Command="{Binding Path=NavigateCommand}"
                CommandParameter="AAAView" />
            <Button
                Content="Navigate To BBBView：导航时向目标View发送消息"
                Margin="10"
                FontSize="18"
                Command="{Binding Path=NavigateCommand}"
                CommandParameter="BBBView" />
            <Button
                Content="Navigate To CCCView：导航拦截，即离开该页面时（前）弹出一个确认对话框"
                Margin="10"
                FontSize="18"
                Command="{Binding Path=NavigateCommand}"
                CommandParameter="CCCView" />
        </StackPanel>

        <ContentControl
            Grid.Row="1"
            prism:RegionManager.RegionName="{x:Static common:Constants.MainViewRegionName }" />

    </Grid>
</Window>
