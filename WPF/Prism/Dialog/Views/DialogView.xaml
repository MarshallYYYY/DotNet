<UserControl
    x:Class="Dialog.Views.DialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Dialog.Views"
    xmlns:viewmodels="clr-namespace:Dialog.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:DialogViewModel}"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800">

    <Grid
        Background="White">

        <Grid.RowDefinitions>
            <RowDefinition
                Height="auto" />
            <RowDefinition />
            <RowDefinition
                Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock
            FontSize="22"
            Text="温馨提示" />

        <TextBlock
            FontSize="50"
            Text="Hello！！！"
            VerticalAlignment="Center"
            Grid.Row="1" />

        <StackPanel
            Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Center">
            <Button
                Content="确定"
                Command="{Binding OKCommand}"
                FontSize="22"
                Margin="10"
                Width="100"
                Height="40" />
            <Button
                Content="取消"
                Command="{Binding CancelCommand}"
                FontSize="22"
                Margin="10"
                Width="100"
                Height="40" />
        </StackPanel>
    </Grid>
</UserControl>
